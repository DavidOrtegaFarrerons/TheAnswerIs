{# templates/home/index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Quizzes • Inicio{% endblock %}

{% block body %}
    <div class="min-h-screen bg-gradient-to-br from-purple-700 via-indigo-700 to-blue-700 text-white">
        <header class="pt-10 pb-24 px-6">
            <div class="max-w-5xl mx-auto text-center space-y-8">
                <span class="text-7xl">🤔</span>
                <h1 class="text-5xl md:text-6xl font-extrabold tracking-tight leading-tight">Pon a prueba tu conocimiento</h1>
                <p class="text-lg md:text-xl text-gray-200 max-w-2xl mx-auto">
                    Explora retos divertidos creados por la comunidad y supera tus límites cada día.
                </p>

                <div class="flex flex-col sm:flex-row justify-center gap-4">
                    <a href="{{ path('contests') }}"
                       class="inline-flex items-center justify-center gap-3 rounded-full bg-yellow-500 px-8 py-4 font-semibold text-neutral-900 transition-transform duration-150 hover:scale-105 hover:bg-yellow-400 active:scale-95">
                        <span>Empezar ahora</span>
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none"
                             viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M14 5l7 7m0 0l-7 7m7-7H3"/>
                        </svg>
                    </a>

                    <a href="#crear"
                       class="inline-flex items-center justify-center gap-3 rounded-full border border-white/30 px-8 py-4 font-semibold transition-all duration-150 hover:bg-white/10 active:scale-95">
                        Crear un quiz
                    </a>
                </div>
            </div>
        </header>

        <main class="space-y-32">
            <section id="quizzes" class="px-6">
                <div class="max-w-6xl mx-auto space-y-8">
                    <h2 class="text-3xl md:text-4xl font-bold text-center">Últimos quizzes</h2>

                    <div class="relative">
                        <div class="overflow-auto scrollbar-hide">
                            <ul class="flex gap-6 snap-x snap-mandatory scroll-pl-6">
                                {% for contest in latestContests %}
                                    <li class="snap-center shrink-0 w-72">
                                        <article
                                            class="rounded-3xl bg-neutral-900/70 backdrop-blur-sm p-6 h-80 flex flex-col gap-6 shadow-xl shadow-black/40">
                                            <h3 class="text-2xl font-bold truncate">{{ contest.name }}</h3>

                                            <div class="relative w-full pt-[56%] rounded-xl overflow-hidden">
                                                <img src="{{ contest.imageUrl }}"
                                                     alt="{{ contest.name }}"
                                                     class="absolute inset-0 w-full h-full object-cover">
                                            </div>

                                            <button type="button"
                                                    onclick="openModal('{{ contest.id }}')"
                                                    class="cursor-pointer inline-flex items-center justify-center gap-2 rounded-full bg-yellow-500 px-6 py-3 font-semibold text-neutral-900 transition-transform duration-150 hover:scale-105 hover:bg-yellow-400 active:scale-95">
                                                Jugar
                                            </button>
                                        </article>
                                    </li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <div id="contest-modal"
         class="fixed inset-0 bg-black/50 hidden items-center justify-center z-50">
        <div class="bg-neutral-900 rounded-2xl p-10 shadow-2xl w-full max-w-sm space-y-6 text-center">
            <h3 class="text-2xl font-bold text-white">¿Crear partida?</h3>
            <p class="text-gray-300">Se generará una nueva partida con el quiz seleccionado.</p>

            <div class="flex justify-center gap-4">
                <button onclick="createGame()"
                        class="cursor-pointer bg-yellow-500 text-neutral-900 px-4 py-2 rounded-full font-semibold hover:bg-yellow-400 transition">
                    Crear partida
                </button>
                <button onclick="closeModal()"
                        class="cursor-pointer bg-white/10 text-white px-4 py-2 rounded-full hover:bg-white/20 transition">
                    Cancelar
                </button>
            </div>
        </div>
    </div>

    <form id="create-game-form"
          method="POST"
          action="{{ path('game_create') }}"
          class="hidden">
        <input type="hidden" name="contestId" id="form-contest-id">
    </form>

    <script>
        let selectedContestId = null;

        function openModal(id) {
            selectedContestId = id;
            const modal = document.getElementById('contest-modal');
            modal.classList.remove('hidden');
            modal.classList.add('flex');
        }

        function closeModal() {
            const modal = document.getElementById('contest-modal');
            modal.classList.remove('flex');
            modal.classList.add('hidden');
            selectedContestId = null;
        }

        function createGame() {
            if (!selectedContestId) return;
            document.getElementById('form-contest-id').value = selectedContestId;
            document.getElementById('create-game-form').submit();
        }
    </script>
{% endblock %}
